<h1>Wikiful: Home of Scholarly Articles</h1>

<p>Here are the five most recently published articles.</p>

<% @recent_articles.each do |article| %>
	
	<div>
		<p><strong><%= article.title%></strong></p>
		<p>Published <%= article.created_at.strftime('%b %d, %Y') %></p>
		<p><em><% article.categories.each do |category| %>
			<%= category.name %>&nbsp;
		<% end %></em></p>
		<p><% truncate(article.content, length: 100)%></p>
	
	</div>
<% end %>